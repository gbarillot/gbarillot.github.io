---
import { Icon } from 'astro-icon/components';
import Button from '~/components/ui/Button.astro';
import Headline from '~/components/ui/Headline.astro';
import WidgetWrapper from '~/components/ui/WidgetWrapper.astro';
import Image from '~/components/common/Image.astro';
import { getColumnsClass } from '~/utils/utils';
import type { Content as Props } from '~/types';

const {
  title = await Astro.slots.render('title'),
  subtitle = await Astro.slots.render('subtitle'),
  tagline,
  content = await Astro.slots.render('content'),
  callToAction,
  items = [],
  columns,
  image = await Astro.slots.render('image'),
  isReversed = false,
  isAfterContent = false,

  id,
  isDark = false,
  classes = {},
  bg = await Astro.slots.render('bg'),
} = Astro.props;

const columnsClass = getColumnsClass(columns);

// `image.aspect` decides the crop: square by default, 4/3, portrait, or the
// natural height without cropping (`auto`).
const { aspect = '1/1', ...imageProps } = typeof image === 'object' && image ? image : { src: '', alt: '' };
const aspectClass = {
  '4/3': 'aspect-[4/3] object-cover',
  '1/1': 'aspect-square object-cover',
  '3/4': 'aspect-[3/4] object-cover',
  auto: '',
}[aspect];
const aspectSize = {
  '4/3': { width: 640, height: 480 },
  '1/1': { width: 640, height: 640 },
  '3/4': { width: 480, height: 640 },
  auto: { width: 640, height: undefined },
}[aspect];
---

<WidgetWrapper
  id={id}
  isDark={isDark}
  containerClass={`max-w-7xl mx-auto ${isAfterContent ? 'pt-0 md:pt-0 lg:pt-0' : ''} ${classes?.container ?? ''}`}
  bg={bg}
>
  <Headline
    title={title}
    subtitle={subtitle}
    tagline={tagline}
    classes={{
      container: 'max-w-xl sm:mx-auto lg:max-w-2xl',
      title: 'text-4xl md:text-5xl font-bold tracking-tighter mb-4 font-heading',
      subtitle: 'max-w-3xl mx-auto sm:text-center text-xl text-muted dark:text-slate-400',
      ...((classes?.headline as Record<string, string>) ?? {}),
    }}
  />
  <div class="mx-auto max-w-7xl px-0 py-4 md:px-2">
    <div class={`md:flex ${isReversed ? 'md:flex-row-reverse' : ''} md:gap-16`}>
      <div class="md:basis-1/2 self-center">
        {content && <div class="mb-12 text-lg dark:text-slate-400" set:html={content} />}

        {
          callToAction && (
            <div class="mt-[-40px] mb-8 text-primary dark:text-blue-300">
              <Button variant="link" {...callToAction} />
            </div>
          )
        }

        {
          items.length > 0 && (
            <div class:list={['grid mx-auto gap-y-4 md:gap-y-8 gap-8', columnsClass]}>
              {items.map(({ title: itemTitle, description, icon, callToAction }) => (
                <div class="intersect-once motion-safe:md:opacity-0 motion-safe:md:intersect:animate-fade">
                  <div class="flex flex-row max-w-none">
                    <div class="flex justify-center">
                      <Icon
                        name={icon || 'tabler:check'}
                        class="flex h-7 w-7 items-center justify-center rounded-full bg-green-600 dark:bg-green-700 text-gray-50 p-1 mr-2 rtl:mr-0 rtl:ml-2"
                      />
                    </div>
                    <div class="mt-0.5">
                      {itemTitle && (
                        <h3 class="text-lg font-medium leading-6 dark:text-white ml-2 rtl:ml-0 rtl:mr-2">
                          {itemTitle}
                        </h3>
                      )}
                      {description && (
                        <p
                          class:list={[{ 'mt-3': itemTitle }, 'text-muted dark:text-slate-400 ml-2 rtl:ml-0 rtl:mr-2']}
                          set:html={description}
                        />
                      )}
                      {callToAction && (
                        <div
                          class:list={[
                            { 'mt-3': itemTitle || description },
                            'text-lg font-medium leading-6 dark:text-white ml-2 rtl:ml-0 rtl:mr-2',
                          ]}
                        >
                          <Button variant="link" {...callToAction} />
                        </div>
                      )}
                    </div>
                  </div>
                </div>
              ))}
            </div>
          )
        }
      </div>
      <div class="mt-10 md:mt-0 md:basis-1/2">
        {
          image && (
            <div class="relative m-auto max-w-4xl">
              {typeof image === 'string' ? (
                <Fragment set:html={image} />
              ) : (
                <Image
                  class:list={['mx-auto w-full rounded-lg bg-gray-500 shadow-lg', aspectClass]}
                  width={aspectSize.width}
                  height={aspectSize.height}
                  widths={[400, 640, 960]}
                  sizes="(max-width: 767px) 100vw, 640px"
                  layout="constrained"
                  format="webp"
                  {...imageProps}
                />
              )}
            </div>
          )
        }
      </div>
    </div>
  </div>
</WidgetWrapper>
